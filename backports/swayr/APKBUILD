# Contributor: Jakub Jirutka <jakub@jirutka.cz>
# Maintainer: Jakub Jirutka <jakub@jirutka.cz>
pkgname=swayr
pkgver=0.13.0_beta1
_pkgver=${pkgver/_beta/-beta.}
pkgrel=0
pkgdesc="A window switcher (and more) for Sway"
url="https://sr.ht/~tsdh/swayr/"
arch="aarch64 armhf armv7 ppc64le x86 x86_64"  # limited by rust/cargo
license="GPL-3.0-or-later"
makedepends="cargo"
source="$pkgname-v$_pkgver.tar.gz::https://git.sr.ht/~tsdh/swayr/archive/v$_pkgver.tar.gz"
builddir="$srcdir/$pkgname-v$_pkgver"

prepare() {
	default_prepare

	# Optimize binary for size.
	cat >> Cargo.toml <<-EOF

		[profile.release]
		codegen-units = 1
		lto = true
		opt-level = "z"
		panic = "abort"
	EOF

	cargo fetch --locked
}

build() {
	cargo build --release --frozen
}

check() {
	cargo test --frozen
}

package() {
	cargo install --locked --offline --path . --root="$pkgdir/usr"
	rm "$pkgdir"/usr/.crates*
}

sha512sums="
369033477a79027a53bf1788ef46414d754373e5e366205c87e58c16193965751a10baf2a6c43755203c383051f2d74cef1ee5c4560dab887dac408d93a78191  swayr-v0.13.0-beta.1.tar.gz
"
